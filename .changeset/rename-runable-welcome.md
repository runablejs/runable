---
"runable": patch
---

Rename the built-in welcome component file to match its public `RunableWelcome` name, load its scoped styles automatically, and keep root lifecycle hooks working during SSR and HMR.
