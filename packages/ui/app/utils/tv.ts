import { ClassValue, createTV, TVReturnTypeLike } from "tailwind-variants";
import { SlotClass } from "../types/tv";

const baseTv = /* @__PURE__ */ createTV({ twMerge: true });

export const tv = /* @__PURE__ */ createTV({ twMerge: true });

// export function tv(config:any) {
//    return baseTv(config)
// }
